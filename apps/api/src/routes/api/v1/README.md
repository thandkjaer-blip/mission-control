# API v1 routes

Current scaffolded routes:
- `GET /api/v1/me`
- `GET /api/v1/overview`
- `GET /api/v1/live` (WebSocket scaffold)

Add future resource route modules here and register them from `index.ts`.
